<template>
  <div>
    <p>智能组件可以称为第三方通用组件，也可以称之为业务型公用组件，与父组件之间的关系是完全解耦的，只能通过 props 进行数据传递，event 进行事件传递，不依赖于任何环境，只需要传递相应的数据和事件，就能得到你想要的操作。</p>
    <p>木偶组件是为了业务页面进行拆分而形成的组件模式。比如一个页面，可以分多个模块，而每一个模块与其余页面并没有公用性，只是纯粹拆分。</p>
    <p>$parent与$children就形成了一个父子组件互相通信的机制,还是那句重点一句只适合木偶组件的模式</p>
  </div>
</template>
<script>
  export default {
    name: "ParentChildren"
  };
</script>